## Process this file with Automake to create Makefile.in.

AUTOMAKE_OPTIONS = -Wno-portability
DEF := $(wildcard *.def)
MAK := $(wildcard *.mak *.dsp)
WIN := $(wildcard *.rc *.ico *.h *.c)

EXTRA_DIST = \
	$(DEF) \
	$(MAK) \
	$(WIN)


